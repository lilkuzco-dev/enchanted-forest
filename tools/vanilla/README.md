# Vanilla texture references

These Minecraft 26.2 block and entity textures are the exact pixel-shape references used by
`tools/generate.js`. The generator changes hue, saturation, and brightness while
preserving the source dimensions, alpha mask, silhouettes, and pixel-level shading.
